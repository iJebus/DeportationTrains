# testing/experimenting
language: python
python:
  - "3.5"
install: "pip install -r requirements.txt"
script:
  - py.test tests.py && python run.py deploy # test export script
  # Phantomjs? Selenium? Neither and use python? - phantomjs testrunner.js
